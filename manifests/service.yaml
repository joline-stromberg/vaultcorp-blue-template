apiVersion: v1
kind: Service
metadata:
  name: vaultcorp
  labels:
    app: vaultcorp
spec:
  type: ClusterIP  # fix: NodePort → ClusterIP, exponera inte direkt på nod-IP
  selector:
    app: vaultcorp
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
